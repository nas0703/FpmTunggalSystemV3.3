# Public Assets
This directory contains static files and assets that are served as-is, such as `favicon.ico`, `manifest.json`, `robots.txt`, etc.

*Note for Vite projects: Unlike Create React App, Vite requires `index.html` to be at the root of the project, not inside `public/`.*
